<!--
Each task description MUST state:
- the behavior or contract being delivered (what is observably true when the
  task is complete), and
- the verification target that proves completion (test, CLI invocation,
  analyzer check, manual assertion, or content review).

File paths are supporting context for locating the work, never the task
itself. "Edit file X" is not a valid task — it is missing both behavior and
verification.
-->

## 1. Shared Data Model & Configuration

- [x] 1.1 Implement the `StockActivityAttributes (shared model)` as defined in the design contract: create `MyTaiwanStock/ActivityKit/StockActivityAttributes.swift` containing a public struct conforming to `ActivityAttributes` with `ContentState` nested struct (`stockNo`, `stockName` static; `currentPrice`, `priceChange`, `priceChangePercent`, `yesterDayPrice`, `time` as `ContentState`). This realizes the design decision for `Shared ActivityAttributes compilation vs App Group serialization`. Verification: the file compiles when added to both the main app target and the StockWidget extension target; the struct conforms to `Codable` and `Hashable` via its `ContentState`.
- [x] 1.2 Add `NSSupportsLiveActivities` key with value `YES` to the main app `MyTaiwanStock/Info.plist` so the system allows the app to manage Live Activities. Verification: right-click Info.plist → Open As → Source Code shows `<key>NSSupportsLiveActivities</key><true/>`; the app does not crash on launch on iOS 15–19.
- [x] 1.3 Add `StockActivityAttributes.swift` to both target memberships (main app and StockWidget extension) in the Xcode project file `MyTaiwanStock.xcodeproj/project.pbxproj`, per the design decision for `Live Activity in StockWidget extension vs new extension`. Verification: building both the app target and the StockWidget extension target succeeds without "undefined symbol" errors.

## 2. ActivityManager (App-Side Lifecycle)

- [x] 2.1 Implement the `ActivityManager API surface` as defined in the design contract: create `ActivityManager` singleton in `MyTaiwanStock/ActivityKit/ActivityManager.swift` with `start(stockNo:stockName:currentPrice:priceChange:priceChangePercent:yesterDayPrice:time:) -> Bool`, `update(currentPrice:priceChange:priceChangePercent:yesterDayPrice:time:)`, and `end()`. All methods guarded with `@available(iOS 16.1, *)`. This realizes the design decision for `Separate ActivityManager singleton vs integrating into StockListViewModel`. Verification: unit test `TestActivityManager` calls start/update/end on an iOS 18 simulator and confirms the activity lifecycle; calling members on iOS 15 simulator logs a message but does not crash.
- [x] 2.2 Implement `Activity.activityStateUpdates` observation in `ActivityManager` so that when the user dismisses the Live Activity from the Lock Screen (swipe), the manager clears its internal activity reference. The manager publishes a Combine `PassthroughSubject<Bool, Never>` (`isActive`) that emits `false` on dismissal. Verification: manually dismiss the activity from Lock Screen on iOS 18 simulator, confirm the `isActive` publisher emits `false`, and the stock detail screen button reverts to "追蹤即時報價".
- [x] 2.3 Implement the design decision for `Single stock tracking vs multiple simultaneous activities`: `ActivityManager.start()` enforces one-activity-at-a-time by calling `end()` on the existing `currentActivity` before creating a new one. Verification: start activity for stock "2330", then start for "0050"; confirm only "0050" activity is visible, the "2330" activity is gone from Lock Screen.

## 3. Live Activity UI (Widget Extension)

- [x] 3.1 Implement the `StockLiveActivity UI contract` per the design document: `Lock Screen display of real-time stock price` showing stock number + name, current price (large `.title` font), price change and change percent with +/- prefix, yesterday reference price, and `HH:mm:ss` timestamp in `Asia/Taipei` timezone per the `Date formatting for display` design decision. Price change and percent use red (`.red`) for positive, green (`.green`) for negative, `.primary` for zero. Implementation in `StockWidget/StockLiveActivity.swift`. Verification: build and launch on iOS 18 simulator with test data (`currentPrice="1060.00"`, `priceChange="+10.00"`), verify Lock Screen shows large "1060.00", red "+10.00" and "+0.95%", reference "1050.00", stock name "2330 台積電", and current timestamp.
- [x] 3.2 Implement `Dynamic Island compact display` in the same `ActivityConfiguration`: compact leading shows stock number (e.g. "2330"), compact trailing shows price change with +/- prefix and red/green color coding. Verification: start the Live Activity, go to Home Screen; confirm Dynamic Island left side shows "2330" and right side shows "+10.00" in red on iOS 18 simulator.
- [x] 3.3 Implement `Dynamic Island minimal display` showing stock number and price change with color coding, for when multiple activities collapse the island. Verification: on iOS 18 simulator with another app providing a Live Activity alongside, confirm this activity's minimal view displays stock number and colored price change.
- [x] 3.4 Implement `Dynamic Island expanded display` showing stock name, current price (large text), price change and percentage with color coding, yesterday reference price, and update timestamp. Verification: long-press the Dynamic Island on iOS 18 simulator, confirm the expanded region shows all fields matching the Lock Screen layout.
- [x] 3.5 Configure `Tap Live Activity opens the app`: the `ActivityConfiguration` uses default `link()` behavior so tapping the Live Activity opens the main app without requiring custom deep-link handling. Verification: tap the Live Activity on Lock Screen of iOS 18 simulator, confirm the main app launches.

## 4. App Integration — Start / Stop Controls

- [x] 4.1 Implement `Start Live Activity from stock detail screen`: add a "追蹤即時報價" button to `StockDetailViewController` that, on iOS 16.1+, starts a Live Activity for the currently viewed stock using `ActivityManager.shared.start(...)` with data from `StockDetailViewModel`. The button is hidden on iOS < 16.1. When already tracking this stock, the button text changes to "停止追蹤" and tapping it calls `ActivityManager.shared.end()`. The button state syncs with `ActivityManager.shared.isActive` via Combine. Verification: open a stock detail on iOS 18 simulator, tap "追蹤即時報價", confirm Live Activity appears on Lock Screen with correct data; tap "停止追蹤", confirm activity disappears. On iOS 15 simulator, confirm the button is not visible.
- [x] 4.2 Implement `Start Live Activity from stock list context menu`: add "開啟即時報價" option to the stock list's `UIContextMenuConfiguration` (long-press context menu) for each cell. The option is only added when running iOS 16.1+. Selecting it calls `ActivityManager.shared.start(...)` with the tapped cell's stock data. Verification: long-press a stock cell on iOS 18 simulator, confirm menu contains "開啟即時報價"; select it, confirm Live Activity starts. On iOS 15 simulator, confirm the option is absent.
- [x] 4.3 Implement `End Live Activity on manual dismissal`: wire up `ActivityManager.shared.isActive` publisher in `StockDetailViewController` so that when the user swipes the Live Activity away from Lock Screen (external dismissal detected by the manager's `activityStateUpdates` observer from task 2.2), the detail screen's button reverts from "停止追蹤" to "追蹤即時報價". Verification: start activity from detail screen, then swipe it away from Lock Screen; return to the app and confirm the button shows "追蹤即時報價".

## 5. Auto-Update & Market Close

- [x] 5.1 Implement `Auto-update Live Activity during polling` per the `App-driven updates via Activity.update() vs APNs push updates` design decision and `Integration with polling` contract: add a call to `ActivityManager.shared.update(...)` in `StockListViewModel.fetchStockInfo(stockNos:)` so that after each successful TWSE API polling response, the view model checks if any `OneDayStockInfoDetail` result matches the tracked stock number; if yes, calls `update(...)` with the formatted `currentPrice`, `priceChange`, `priceChangePercent`, `yesterDayPrice`, and `time` values. Verification: start a Live Activity for a stock, wait for the next 60-second polling cycle, confirm the Live Activity price and timestamp update within 2 seconds of the in-app UI update on iOS 18 simulator.
- [x] 5.2 Ensure `StockListViewModel` does NOT update the Live Activity when the tracked stock is absent from the polling results (e.g., user switched to a different watchlist tab). Verification: start Live Activity for stock "2330", switch to a tab that does not contain "2330", wait for polling; confirm the Live Activity retains its last values and is not updated with irrelevant data.
- [x] 5.3 Implement `Auto-end activity on Taiwan market close` per the `Market close detection` design contract: track the `time` field from consecutive polling responses. After 13:30 Taipei time, if the API `time` field remains unchanged for two consecutive cycles (120 seconds), call `ActivityManager.shared.end()`. Do NOT auto-end before 13:30 even if the time field is stale (handles trading halts). Verification: mock two consecutive API responses with identical `time="13:30:00"` at system time 13:31 on iOS 18 simulator; confirm the Live Activity is dismissed. Mock identical time at 11:00; confirm Live Activity is NOT dismissed.

## 6. Build & Compatibility Verification

- [x] 6.1 Verify all `Acceptance criteria` from the design document, staying within the `Scope boundaries`: Build the main app target with iOS 15.2 deployment target on a simulator running iOS 15.x (`xcodebuild -destination 'platform=iOS Simulator,name=iPhone 13,OS=15.5' build`). Verification: build succeeds with zero errors; the app launches and all existing features (stock list, detail, widget) work normally without crashes.
- [x] 6.2 Build and run on an iOS 18.x simulator, exercise the full Live Activity flow: start from detail screen, verify Lock Screen and Dynamic Island display, wait for polling update, end from detail screen. Verification: all views render correctly with Taiwan convention colors (red up, green down), data matches in-app display, activity dismisses cleanly, and the `Acceptance criteria` in the design document are fully satisfied.

## 7. 實機測試發現的修復（Post-Implementation Hardening）

<!-- 這批 task 是在 6.x 完成後，於實體 iPhone 上測試 widget 與 Live Activity 時發現並修復的問題，非原始 19 項任務範圍，但與本 change 直接相關。 -->

- [x] 7.1 修正 `toggleLiveActivity()` 用全域 `ActivityManager.shared.isActive` 判斷是否已在追蹤，導致從股票 A 的畫面切到股票 B 再按按鈕時，會誤把 A 的 Live Activity 關掉而非為 B 開新的。改為比對 `ActivityManager.shared.trackedStockNo == viewModel.stockNo`，`StockViewController` 的按鈕圖示同步邏輯也一併修正。Verification: 對股票 A 啟動 Live Activity，導覽到股票 B 的詳情頁，確認按鈕圖示顯示未追蹤狀態；點擊後應為 B 啟動新的 Live Activity 而非結束 A 的。
- [x] 7.2 修正 `startLiveActivity(from:)` 計算 `priceChangePercent` 正負號時，誤用 `diffStr`（漲跌價）的正負號去決定要不要加 `+`，卻套用在基準不同的 `percentStr`（漲跌幅）上，兩者正負號不一致時會產生如 `"+-2.857%"` 的畸形字串顯示在 Live Activity。改為直接解析 `percentStr` 自身數值判斷正負號。Verification: 建構 current/open/yesterday 價格使漲跌價為正、漲跌幅為負的測試案例，確認產生的字串不含 `"+-"`。
- [x] 7.3 修正 `StockListViewModel.fetchStockInfo` 的 Combine chain 缺少 `.receive(on: DispatchQueue.main)`，導致 `updateLiveActivityIfNeeded()`／`checkMarketClose()` 在背景執行緒呼叫 `ActivityManager`，與主執行緒的 UI 操作（`start`/`end`）競爭同一個非執行緒安全的 `currentActivity` 屬性。加上 `.receive(on: DispatchQueue.main)`。Verification: 確認 `fetchStockInfo` 的 `.sink` 前有 `.receive(on: DispatchQueue.main)`；Thread Sanitizer 執行不再對 `ActivityManager.currentActivity` 報告 race。
- [x] 7.4 修正 `checkMarketClose()` 用清單第一支股票（`onedayStockInfo.first?.time`）的時間戳判斷是否已收盤，而非真正被 Live Activity 追蹤的股票，與 `updateLiveActivityIfNeeded()` 用 `trackedStockNo` 查找的邏輯不一致。改為 `onedayStockInfo.first(where: { $0.stockNo == trackedStockNo })?.time`，並在沒有 `trackedStockNo` 時直接 return。Verification: 追蹤的股票不是清單第一支時，收盤判斷仍應以該股票自己的時間戳為準。
- [x] 7.5 修正 widget 在 iOS 17+ 未呼叫必要的 `containerBackground`（透過 `View+Extension.swift` 的 `widgetBackground` 包裝），導致 widget 背景無法正確套用。補回 `.widgetBackground(Color(.systemBackground))` 呼叫。Verification: 在 iOS 17+ 實機上加入 widget，背景正確顯示系統背景色而非未套用背景樣式的警告狀態。
- [x] 7.6 修正 `NetworkServiceImpl.fetchOneDayStockInfo` 在 `URLSession` 回呼 `data` 為 `nil`（網路逾時/無網路）時完全不呼叫 `completionHandler`，導致呼叫端（尤其是 widget 的 `getTimeline`）永遠等不到結果，系統因此永久顯示 redacted 佔位骨架畫面。改為 `guard let data else { completionHandler(.failure(...)); return }`，確保任何失敗路徑都會回呼。Verification: 模擬 `data=nil, error=某錯誤` 的情境，確認 `completionHandler` 被呼叫且 `Result` 為 `.failure`。
- [x] 7.7 移除 `StockWidgetExtension` target 對 Firebase（`FirebaseAuth`/`FirebaseFirestore`/`FirebaseFirestoreSwift`）與 `GoogleSignIn`/`GoogleSignInSwift` 的直接連結，以及因 `NetworkServiceImpl`（宣告 `lazy var onLineDBService = OnlineDBService()`／`lazy var localDBService = LocalDBService.shared`）而被迫一併編譯進 widget target 的 `OnlineDBService.swift`、`LocalDBService.swift`、Core Data model（`MyTaiwanStock.xcdatamodeld`）與其生成類別。widget 完全用不到這些依賴，卻在只有 30MB 記憶體上限的 extension process 裡佔用大量空間。新增 `StockWidget/WidgetStockFetcher.swift`，一個只做 `URLSession` 請求＋JSON decode 的最小型別，取代 widget 對 `NetworkServiceImpl` 的依賴。Verification: `StockWidgetExtension.appex` 建置產物大小從數十 MB 降到約 836KB；widget target 的 `packageProductDependencies` 不再包含 Firebase/GoogleSignIn。
- [x] 7.8 修正 widget 讀到的 App Group `stockNos` 陣列含有空字串，導致 widget 拿空字串去問 TWSE API，API 回傳 `"c":""`／`"z":"-"` 的空資料，畫面顯示空白股票名稱與 `-`／`0.00`。根本原因有二：(a) `setupStockNameStringSet()` 把 Core Data 裡 `stockNo` 為 `nil` 的記錄用 `map`+`guard-else` 轉成空字串 `""` 而非過濾掉，改為 `compactMap`；(b) App 冷啟動的正常抓取流程（`setupFetchStockInfo` 的 `currentMenuIndex` sink）從未把股票清單寫回 App Group／呼叫 `WidgetCenter.shared.reloadAllTimelines()`，只有切換清單／新增／刪除股票才會同步，導致舊的壞資料永遠不會被修正，新增此同步呼叫。另外在 widget 端的 `retrieveStockNos()` 加上 `.filter { !$0.isEmpty }` 作為防禦層。Verification: 實機上刪除並重新加入 widget，桌面 widget 顯示與 App 內清單一致的真實股票代號、名稱與價格。
- [x] 7.9 widget 顯示層加上「無即時成交價時 fallback 顯示昨日收盤價」與「股票簡稱為空時 fallback 顯示股票代號」，涵蓋非交易時段 API 回傳 `"z":"-"` 但 `"y"`（昨收）仍有值的情境。Verification: 建構 `current="-"`、`yesterDayPrice="130.00"` 的 `WidgetStockData`，確認畫面顯示 `"130.00"` 而非 `"-"`。
- [x] 7.10 修正 7.8 把 `setupStockNameStringSet()` 的 `map`+`guard-else` 改成 `compactMap` 時，遺留的 `(ele as? StockNo)?.stockNo` 型別轉型：實際被疊代的陣列型別是 `[StockNoStruct]`（一般 struct），與 Core Data 的 `StockNo` class 無關，此轉型必定失敗。在舊的 `map`+`guard-else` 寫法下這會靜默產生全空字串陣列（7.8 要修的原始 bug）；換成 `compactMap` 後則直接產生空陣列，導致從選單切換清單時把 widget 同步的股票清單整個清空。改為直接讀取 struct 上的 `stockNo` 屬性，與檔案中其他地方的既有寫法一致。Verification: 從選單切換追蹤清單，確認 widget 的股票清單維持與新清單一致而非變空。
- [x] 7.11 移除 `setupStockNameStringSet()` 造成的重複同步：切換清單的選單動作原本會依序呼叫 `currentMenuIndex.send(index)` 與 `setupStockNameStringSet()`，但自 0ca4359 起 `currentMenuIndex` 的 Combine sink 本身就已經會把 `stockNos` 寫入 App Group 並呼叫 `WidgetCenter.shared.reloadAllTimelines()`，導致每次切換清單都對同一份資料寫入與重新整理 widget 兩次，且兩次 `UserDefaults` 寫入互相競爭。`setupStockNameStringSet()` 只有這一處呼叫點，故整個移除而非留下死碼。Verification: 切換清單時確認 App Group 的 `stockNos` 與 `WidgetCenter.shared.reloadAllTimelines()` 只各觸發一次。
- [x] 7.12 修復 code review 對 7.7 Firebase 依賴清理的後續發現：(a) widget 在 API 回傳成功但 `msgArray` 為空時改為 fallback 到 `placeholderEntry()`，而非渲染沒有任何提示的空白卡片；(b) 移除 `shortName` 為空時 fallback 顯示 `stockNo` 的邏輯，該邏輯會讓 widget 同一列直向重複顯示兩次股票代號；(c) 將 `urlComponents.url!` 的強制解包改為 `guard let`，失敗時回傳 `.failure` 而非讓 extension process 直接 crash；(d) 把原本分別存在於 `NetworkServiceImpl` 與 widget-only `WidgetStockFetcher` 的 TWSE 抓取邏輯（URL 組裝、query 參數、decode）抽成單一 `MyTaiwanStock/Repository/TWSEStockInfoFetcher.swift`，同時編譯進 app 與 widget extension 兩個 target，`NetworkServiceImpl` 改為委派給它，避免兩份邏輯漂移不同步；(e) 清理 `project.pbxproj` 中因先前 widget target 依賴清理（7.7）而遺留的 13 筆孤兒 `PBXBuildFile` 項目（Firebase、GoogleSignIn、Core Data model 檔案、重複的 `NetworkServiceImpl`／`NetworkService` 項目）。Verification: 模擬 API 成功但 `msgArray` 為空，確認 widget 顯示 placeholder 而非空白卡片；確認 widget 股票列不再重複顯示股票代號兩次；`project.pbxproj` 中不再有指向已移除依賴的孤兒 `PBXBuildFile` 項目。
- [x] 7.13 修正 widget 「前三支股票」清單順序不穩定的問題：舊行為下 widget 永遠截取同步清單的前 3 筆，但清單順序取決於最後是哪個路徑寫入——切換清單路徑保留 Core Data 關聯（無序）的原始順序，而新增／刪除股票的路徑則透過 `Set` 往返（完全無序），使得新增或刪除一支不相關的股票後，widget 顯示的前 3 支股票可能洗牌。在寫入 App Group 的三個同步點都改為依股票代號排序（`.sorted()`），與主清單預設（未搜尋時依 `stockNo` 排序）的排序方式一致。Verification: 新增或刪除一支不在 widget 前 3 名內的股票，確認 widget 顯示的前 3 支股票與其順序不受影響，且與 App 內清單畫面最上方 3 支股票一致。
